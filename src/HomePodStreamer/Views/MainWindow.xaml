<Window x:Class="HomePodStreamer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="HomePod Streamer"
        Height="550" Width="500"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>

        <!-- System Tray Icon -->
        <tb:TaskbarIcon x:Key="NotifyIcon"
                        x:Name="NotifyIcon"
                        ToolTipText="HomePod Streamer"
                        TrayMouseDoubleClick="NotifyIcon_TrayMouseDoubleClick">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Show Window" Click="ShowWindow_Click"/>
                    <MenuItem Header="{Binding StreamingButtonText}" Click="ToggleStreaming_Click"/>
                    <Separator/>
                    <MenuItem Header="Quit" Click="Quit_Click"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="{Binding StatusMessage}"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap"
                       Width="320"/>
            <Button Content="Refresh Devices"
                    Command="{Binding RefreshDevicesCommand}"
                    Padding="10,5"
                    Margin="10,0,0,0"
                    MinWidth="120"/>
        </StackPanel>

        <!-- Streaming Toggle -->
        <Button Grid.Row="1"
                Content="{Binding StreamingButtonText}"
                Command="{Binding ToggleStreamingCommand}"
                Height="45"
                FontSize="16"
                FontWeight="SemiBold"
                Margin="0,0,0,20">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="#2196F3"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsStreaming}" Value="True">
                            <Setter Property="Background" Value="#F44336"/>
                        </DataTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.8"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <!-- Device List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Devices}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDDDDD"
                                BorderThickness="1"
                                CornerRadius="5"
                                Padding="15"
                                Margin="0,0,0,10"
                                Background="White">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <CheckBox Grid.Column="0"
                                          IsChecked="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          VerticalAlignment="Center"
                                          Click="DeviceCheckBox_Click"/>

                                <StackPanel Grid.Column="1" Margin="15,0">
                                    <TextBlock Text="{Binding Name}"
                                              FontWeight="Bold"
                                              FontSize="14"/>
                                    <TextBlock Text="{Binding IPAddress}"
                                              FontSize="11"
                                              Foreground="Gray"
                                              Margin="0,2,0,0"/>
                                </StackPanel>

                                <TextBlock Grid.Column="2"
                                          Text="{Binding State}"
                                          VerticalAlignment="Center"
                                          FontSize="12">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="Gray"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding State}" Value="Connected">
                                                    <Setter Property="Foreground" Value="Green"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding State}" Value="Streaming">
                                                    <Setter Property="Foreground" Value="Blue"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding State}" Value="Error">
                                                    <Setter Property="Foreground" Value="Red"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Volume Control -->
        <StackPanel Grid.Row="3" Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                          Text="Volume"
                          VerticalAlignment="Center"
                          FontWeight="SemiBold"
                          Margin="0,0,10,0"/>

                <Slider Grid.Column="1"
                        Value="{Binding Volume, Mode=TwoWay}"
                        Minimum="0"
                        Maximum="100"
                        TickFrequency="5"
                        IsSnapToTickEnabled="True"
                        VerticalAlignment="Center"/>

                <TextBlock Grid.Column="2"
                          Text="{Binding Volume}"
                          VerticalAlignment="Center"
                          FontWeight="Bold"
                          Width="30"
                          TextAlignment="Right"
                          Margin="10,0,0,0"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
